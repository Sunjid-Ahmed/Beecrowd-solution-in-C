#include<stdio.h>
#include<math.h>
main()
{
    double n,h,c,l,s;
    while(scanf("%lf",&n)==1){
        scanf("%lf %lf %lf",&h,&c,&l);
        s=sqrt((h*h)+(c*c));
        s*=n*l;
        s=s/10000;
        printf("%.4lf\n",s);
    }
}
